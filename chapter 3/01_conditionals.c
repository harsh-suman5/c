#include<stdio.h> // this is a header file
int main()
{
    int age = 15; // this is a integer value which is denoted as age
   if (age>10)
   {
      printf("we are inside if\n");
      printf("your age is greater than 10\n");
   }
   if (age%5==0) // this is a if condition 
   {
      printf("we are inside  another if\n"); // this is an input function
      printf("your age is divisible by 50\n"); // this is also an input function 
   }
   return 0;
    
}