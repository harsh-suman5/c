#include <stdio.h>
int main (){
	// c language is a case-sensitive
int area, base, height;
//whitespace
 printf("enter the value : ");
 scanf("%d%d", &base,&height);
 area =base*height;
 printf("\nthe value is:%d",area);
 // \n is for new line 
 return 0;
 }
