// binary AND operator
#include <stdio.h>
#include <conio.h>
int main(){
	int a = 6, b = 14;
	
	  printf("The output of the Bitwise AND operator a&b is %d\n",a&b);  
	  getch();
	  printf(" the output are correctly run by the compiler");
   
     return 0;
}
